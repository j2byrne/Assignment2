void playerTurn(struct slots slot[], struct players player[])
{
	for (size_t i = 0; i < playerNumber-1; i++)
	{
		int slotBefore = (slotNumber-2 + i ) % slotNumber;
		int slotAfter = (slotNumber + i ) % slotNumber;
	}
}
